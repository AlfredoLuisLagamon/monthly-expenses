/** 8dp grid – use multiples for consistent spacing (Pixel-style) */
export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 12,
  base: 16,
  lg: 20,
  xl: 24,
  xxl: 32,
} as const;

export const borderRadius = {
  sm: 8,
  md: 12,
  lg: 16,
  full: 9999,
} as const;

export const iconSize = {
  sm: 20,
  md: 24,
  lg: 28,
} as const;
